{
    "name": "community-security-alert-root",
    "version": "1.0.0",
    "scripts": {
        "install-all": "npm install --prefix backend && npm install --prefix frontend",
        "start-back": "npm start --prefix backend",
        "start-front": "npm start --prefix frontend",
        "build-front": "npm run build --prefix frontend",
        "dev": "concurrently \"npm run start-front\" \"npm run start-back\""
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    },
    "dependencies": {
        "cors": "^2.8.6"
    }
}
